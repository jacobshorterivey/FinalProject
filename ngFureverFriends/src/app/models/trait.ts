export class Trait {

  // FIELDS
  private id?: number;
  private description?: string;

  // CONSTRUCTOR
  constructor(id?: number, description?: string) {
    this.id = id;
    this.description = description;
  }
}
