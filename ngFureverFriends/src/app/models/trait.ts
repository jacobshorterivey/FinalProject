export class Trait {

  // FIELDS
  id?: number;
  description?: string;

  // CONSTRUCTOR
  constructor(id?: number, description?: string) {
    this.id = id;
    this.description = description;
  }
}
