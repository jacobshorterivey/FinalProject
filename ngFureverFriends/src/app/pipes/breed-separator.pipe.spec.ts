import { BreedSeparatorPipe } from './breed-separator.pipe';

describe('BreedSeparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new BreedSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
