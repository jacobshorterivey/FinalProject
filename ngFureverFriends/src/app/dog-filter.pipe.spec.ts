import { DogFilterPipe } from './dog-filter.pipe';

describe('DogFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DogFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
